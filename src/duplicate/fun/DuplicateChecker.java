package duplicate.fun;

public interface DuplicateChecker {

	/**
	 * Check if a String contains duplicate characters.
	 * @param input - The string to check
	 * @return true if duplicate is found or false if there are no duplicates.
	 */
	public boolean containsDuplicate(String input);
	
}
